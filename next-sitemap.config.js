const config = {
  siteUrl: 'https://bermelgeorgis.vercel.app',
  generateRobotsTxt: true, // Generate a robots.txt file
  changefreq: 'weekly', // Frequency of changes (optional)
  priority: 0.7, // Page priority (optional)
};

export default config;
